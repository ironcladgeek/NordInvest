# FalconSignals Environment Variables Sample
# Copy this file to .env and fill in your actual API keys
# DO NOT commit the .env file to version control

# LLM API Keys (choose one - Anthropic is recommended)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here  # Optional alternative to Anthropic

# Financial Data APIs (optional - free tiers supported)
FINNHUB_API_KEY=your_finnhub_api_key_here
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key_here

# Configuration file path (optional - defaults to config/local.yaml)
# CONFIG_FILE=config/local.yaml

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Data directory for cache and reports (optional - defaults to ./data)
# DATA_DIR=data

# Deployment/scheduling settings (optional)
# RUN_TIME=08:00  # UTC time for daily runs
# TIMEZONE=Europe/Stockholm
# COST_LIMIT_EUR_PER_MONTH=100
